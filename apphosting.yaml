# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
  # Set the request timeout to 1 hour to allow for long-running AI searches.
  timeoutSeconds: 3600

# Required for Genkit to be able to access the Google AI API
# Temporarily commented out to resolve provisioning issues.
# secretEnv:
#   - secret: GEMINI_API_KEY
#     availability:
#       - BUILD
#       - RUNTIME
